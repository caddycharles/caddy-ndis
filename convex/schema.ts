import { defineSchema } from "convex/server";

// Placeholder schema - will be fully defined in Story 2.1
export default defineSchema({
  // Schema will be populated with tables in upcoming stories
});